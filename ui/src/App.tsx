import Console from '@/components/Console'

/** Один экран, один режим.
 *
 *  Прежде здесь стоял выбор между мастером и подробным видом, и делался он САМ — по факту
 *  наличия каналов в спеке. Человек, у которого настройка появилась, терял мастер и не понимал,
 *  куда тот делся; человек с пустой спекой не видел подробного вида и не знал, что тот есть.
 *  Автоматическое переключение режимов оказалось хуже любого из режимов.
 *
 *  Теперь режим один: состояние закреплено слева, работа справа, а глубина открывается по мере
 *  надобности — редактор правила раскрывается на месте таблицы, каталог ищется поиском. */
export default function App() {
    /* Архив настроек ЖИЛ ЗДЕСЬ — под пультом и на всю ширину, с доводом «он не про
       маршрутизацию, а про весь экран целиком». Довод верный, а вывод из него был неверный:
       карточка висела на КАЖДОМ экране, включая те, где до архива дела нет, и занимала место
       постоянно ради действия, которое делают раз в жизни. Теперь она во вкладке движка,
       рядом с остальным, что не про маршрутизацию (EngineCard, SelfUpdateCard).

       Прежний довод при этом никуда не делся и выполняется: вкладка доступна всегда, в том
       числе когда настройка ещё не работает и переносить её надо на новый роутер. */
    return <Console />
}

/** Разделы пульта. Отдельным файлом, а не в оболочке: тип нужен и рельсу, и каждому разделу,
 *  который умеет отправить человека в соседний («остаток трафика — на обзоре», «выход
 *  создаётся в выходах»), а импорт из оболочки в её же ребёнка — это круг. */
export type SectionId = 'overview' | 'rules' | 'outputs' | 'catalog' | 'diag' | 'system'

/** Заголовки разделов. Здесь, а не в рельсе, потому что раздел печатает своё имя сам —
 *  и оно обязано совпадать с пунктом рельса дословно. */
export const SECTION_TITLE: Record<SectionId, string> = {
    overview: 'Обзор',
    rules: 'Правила',
    outputs: 'Выходы',
    catalog: 'Каталог',
    diag: 'Диагностика',
    system: 'Система',
}
